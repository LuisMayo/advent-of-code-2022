fn main() {
    println!("All examples should be run from within the project's root. Example:");
    println!("`cargo run --bin d${{day}}p${{part}} ${{sample|full}}`");
    println!("For instance");
    println!("`cargo run --bin d1p2 full`");
}
